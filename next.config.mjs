/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  eslint: {
    // যদি উপরের ফিক্স কাজ না করে তবেই এটি ব্যবহার করবেন (সাময়িকভাবে বিল্ড চালু করতে)
    // ignoreDuringBuilds: true,
  },
};

export default nextConfig;
